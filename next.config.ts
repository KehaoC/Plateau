import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: ['tfstbtekfqrflkcukpzg.supabase.co']
  },
  eslint: {
    ignoreDuringBuilds: true,
  }
}

export default nextConfig;
